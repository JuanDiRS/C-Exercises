#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int main()
{
    int numero contador, digitos, temporal,i;
    cout<<"ingrese un numero de 4 digitos: ";
    cin>>numero;
    
    temporal = numero;
    contador = 0;
    
    //contamos la cantidad de numeros y se almacena en contador.
    while(temporal > 0){
        temp /= 10; //para sacar el ultimo digito
        contador ++;
    }
    
    int digitos[contador];  //se declara un array llamado digitos de tamaño contador
    temporal = numero;//Volver a copiar el numero original en temporal
    
    for(i)
}
